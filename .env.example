# URL the browser uses to reach the backend (baked into the JS bundle at build time)
NEXT_PUBLIC_API_URL=http://localhost:8080

# URL the Next.js server uses to reach the backend (runtime, can differ from above)
BACKEND_URL=http://localhost:8080

# Set to "true" to enable API request/response logging in the browser console
NEXT_PUBLIC_DEBUG=false
